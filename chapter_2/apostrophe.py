#Here is how you use single and double quotes correctly :
#The apostrophe appears inside a set of double quotes, so the Python -
#interpreter has no trouble reading the string correctly

message = "One of Python's strengths is its diverse community."

print(message)

#can't do something like this :
# 'One of python's strengths ...'    --because the python interpreter won't know where the strings start and end.
